# Lint and format. Types are checked by basedpyright (pyrightconfig.json), not by ruff.
# The editor reads this file too (.vscode/settings.json), so both report the same.
target-version = "py313"
line-length = 88
src = ["packages/*/src", "packages/*/tests", "apps/*/src", "apps/*/tests"]

[lint]
select = ["ALL"]
preview = true
future-annotations = true
ignore = [
    "single-line-implicit-string-concatenation",
    "missing-trailing-comma",
    "incorrect-blank-line-before-class",
    "multi-line-summary-second-line",
    "undocumented-public-init",
    "undocumented-public-package",
    "missing-copyright-notice",
    "unused-async",
    "ambiguous-unicode-character-string",
    "ambiguous-unicode-character-docstring",
    "ambiguous-unicode-character-comment",
]

[lint.pydocstyle]
convention = "google"

[lint.isort]
required-imports = ["from __future__ import annotations"]
known-first-party = ["vld"]

[lint.flake8-type-checking]
# pydantic and dishka read annotations at runtime: their imports must stay importable.
runtime-evaluated-base-classes = [
    "pydantic.BaseModel",
    "pydantic_settings.BaseSettings",
]
runtime-evaluated-decorators = ["dishka.provide", "dishka.integrations.fastapi.inject"]

[lint.per-file-ignores]
# dishka providers are methods by design, even when they do not use `self`, and
# dishka always finalizes a generator provider, so a yield inside `async with` is safe.
"**/di.py" = ["no-self-use", "yield-in-context-manager-in-async-generator"]
"**/di/**" = ["no-self-use", "yield-in-context-manager-in-async-generator"]
# FastAPI resolves the annotations of routes and dependencies at runtime: a `Request`
# moved under `if TYPE_CHECKING` turns into a query parameter and every call gets 422.
"packages/web/src/vld/web/access/**" = [
    "typing-only-first-party-import",
    "typing-only-standard-library-import",
    "typing-only-third-party-import",
]
"packages/*/src/vld/*/api/**" = [
    "typing-only-first-party-import",
    "typing-only-standard-library-import",
    "typing-only-third-party-import",
]
# Tests need a one-line docstring, not Args/Returns sections.
"**/tests/**" = [
    "ANN",
    # pytest imports test modules by path (--import-mode=importlib).
    "implicit-namespace-package",
    "DOC",
    "assert",
    "import-private-name",
    "line-too-long",
    "magic-value-comparison",
    "no-self-use",
    "private-member-access",
]

[format]
preview = true
docstring-code-format = true
